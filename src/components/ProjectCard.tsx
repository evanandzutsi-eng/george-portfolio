import {ArrowUpRight, Github, ExternalLink, Sparkles} from 'lucide-react';import {Link} from 'react-router-dom';import {projects} from '../data/site'
export function ProjectCard({project}:{project:typeof projects[number]}){return <article className="project-card"><div className="project-visual"><div className="visual-grid"></div><div className="visual-mark"><Sparkles size={22}/><span>ENGINEERED<br/>FOR USEFUL AI</span></div><span className="project-status">{project.status}</span><span className="project-index">/{String(projects.indexOf(project)+1).padStart(2,'0')}</span></div><div className="project-body"><div className="tag-row">{project.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div><h3>{project.title}</h3><p>{project.description}</p><div className="stack-row">{project.stack.map(s=><span key={s}>{s}</span>)}</div><div className="card-actions"><Link to={`/projects/${project.slug}`} className="text-link">Case Study <ArrowUpRight size={15}/></Link>{project.links.github&&project.links.github!=='#'&&<a className="icon-link" href={project.links.github} target="_blank" rel="noreferrer" aria-label="GitHub"><Github size={18}/></a>}{project.links.live&&project.links.live!=='#'&&<a className="icon-link" href={project.links.live} target="_blank" rel="noreferrer" aria-label="Live demo"><ExternalLink size={18}/></a>}</div></div></article>}
