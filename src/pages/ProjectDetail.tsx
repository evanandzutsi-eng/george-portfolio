import {Link,useParams} from 'react-router-dom';import {ArrowLeft,ArrowUpRight,ExternalLink} from 'lucide-react';import {projects} from '../data/site';import {Architecture} from '../components/Architecture';import {ProjectEvidence} from '../components/ProjectEvidence'
export function ProjectDetail(){const {slug}=useParams();const p=projects.find(x=>x.slug===slug);if(!p)return <div className="container not-found"><h1>Project not found.</h1><Link to="/projects" className="text-link">Back to projects</Link></div>;return <><section className="detail-hero"><div className="container"><Link to="/projects" className="back-link"><ArrowLeft size={15}/> Projects</Link><p className="eyebrow">{p.category.toUpperCase()} · {p.status.toUpperCase()}</p><h1>{p.title}</h1><p className="lead">{p.description}</p><div className="tag-row">{p.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div></div></section><section className="section"><div className="container detail-grid"><article><ProjectEvidence project={p}/><div className="detail-block"><p className="eyebrow">01 · PROBLEM</p><h2>What needed to be solved</h2><p className="body-copy">{p.problem}</p></div><div className="detail-block"><p className="eyebrow">02 · SOLUTION</p><h2>How the system is approached</h2><p className="body-copy">{p.solution}</p></div><div className="detail-block"><p className="eyebrow">03 · ARCHITECTURE</p><p className="body-copy">{p.architecture}</p><Architecture/></div><div className="detail-block"><p className="eyebrow">04 · ENGINEERING DECISIONS</p><ul className="clean-list">{p.decisions.map(d=><li key={d}>{d}</li>)}</ul></div><div className="detail-block"><p className="eyebrow">05 · SECURITY</p><p className="body-copy">{p.security}</p></div><div className="detail-block"><p className="eyebrow">06 · DEPLOYMENT</p><p className="body-copy">{p.deployment}</p></div><div className="detail-block"><p className="eyebrow">07 · OUTCOME</p><p className="body-copy">{p.outcome}</p></div></article><aside className="detail-aside"><div className="aside-card"><p className="eyebrow">TECHNOLOGY</p>{p.stack.map(s=><span className="stack-pill" key={s}>{s}</span>)}<div className="aside-actions">{p.links.github&&<a className="button button-small" href={p.links.github} target="_blank" rel="noreferrer"><ExternalLink size={15}/> GitHub</a>}{p.links.live&&<a className="button button-small button-ghost" href={p.links.live} target="_blank" rel="noreferrer"><ExternalLink size={15}/> Live Demo</a>}</div></div></aside></div></section><section className="section cta-section"><div className="container cta-box"><div><p className="eyebrow">BUILD SOMETHING SIMILAR?</p><h2>Let's discuss the real problem.</h2></div><Link to="/contact" className="button">Start a Project <ArrowUpRight size={16}/></Link></div></section></>}
