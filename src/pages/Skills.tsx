import {useState} from 'react';import {PageHero} from '../components/Section';import {skills} from '../data/site';import {Code2,Database,Cloud,ShieldCheck,WandSparkles,Workflow,ChevronRight} from 'lucide-react'
const icons=[Code2,WandSparkles,Code2,WandSparkles,Database,Cloud,Workflow,ShieldCheck]
export function Skills(){const [active,setActive]=useState(0);return <><PageHero eyebrow="TECHNICAL STACK" title="Tools I use to turn ideas into systems." description="A practical technology map. Skill depth is intentionally described through actual use cases instead of arbitrary percentage bars."/><section className="section"><div className="container skill-layout"><div className="skill-nav">{skills.map((s,i)=>{const I=icons[i];return <button className={active===i?'skill-nav-item active':'skill-nav-item'} key={s.category} onClick={()=>setActive(i)}><span><I size={17}/>{s.category}</span><ChevronRight size={16}/></button>})}</div><div className="skill-detail"><p className="eyebrow">{skills[active].category.toUpperCase()}</p><h2>{skills[active].category}</h2><p className="lead">Technologies and practices used across projects, experiments and engineering workflows.</p><div className="skill-chips">{skills[active].items.map(x=><div className="skill-chip" key={x}><span></span>{x}</div>)}</div></div></div></section><section className="section section-dark"><div className="container"><p className="eyebrow">SECURITY MINDSET</p><h2>Security belongs in the design, not the checklist at the end.</h2><p className="lead narrow">Authentication, validation, API security, secrets management, secure headers and OWASP-informed practices are part of how I think about software.</p></div></section></>}
