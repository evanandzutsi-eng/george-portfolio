import {Github,Linkedin,Play,ExternalLink} from 'lucide-react';import {PageHero} from '../components/Section';import {site,isPlaceholder} from '../data/site'
export function Media(){const items=[{source:'YouTube',title:'Video channel',desc:'Connect your official YouTube channel and selected videos here.',icon:Play,url:site.youtube},{source:'LinkedIn',title:'Professional updates',desc:'Share engineering lessons, project updates and professional writing.',icon:Linkedin,url:site.linkedin},{source:'GitHub',title:'Open-source work',desc:'Explore public repositories and engineering activity.',icon:Github,url:site.github}];return <><PageHero eyebrow="MEDIA & CONTENT" title="Latest from me." description="A home for videos, technical posts, professional updates and public engineering work."/><section className="section"><div className="container media-grid">{items.map(x=>{const disabled=isPlaceholder(x.url);return <article className="media-card" key={x.source}><div className="media-icon"><x.icon size={23}/></div><span className="eyebrow">{x.source}</span><h2>{x.title}</h2><p>{x.desc}</p>{disabled?<span className="muted small">Add the official URL in <code>src/data/site.ts</code>.</span>:<a className="text-link" href={x.url} target={x.url.startsWith('http')?'_blank':undefined} rel="noreferrer">Open source <ExternalLink size={15}/></a>}</article>})}</div></section><section className="section section-alt"><div className="container"><p className="eyebrow">CONTENT ARCHITECTURE</p><h2>One place for the work behind the brand.</h2><p className="lead narrow">When platform APIs or embeds are limited, this site uses lightweight preview cards that link directly to the original source rather than copying third-party content.</p></div></section></>}
