import {Link} from 'react-router-dom';import {ArrowUpRight} from 'lucide-react';import {PageHero} from '../components/Section';import {projects} from '../data/site'
export function CaseStudies(){return <><PageHero eyebrow="CASE STUDIES" title="How the engineering gets done." description="Technical stories that separate verified project facts from engineering interpretation, so visitors can understand the problem, architecture, implementation and outcome without inflated claims."/><section className="section"><div className="container case-grid">{projects.map((p,i)=><article className="case-card" key={p.slug}><span>CASE STUDY / 0{i+1}</span><h2>{p.title}</h2><p>{p.description}</p><div className="case-flow"><b>Problem</b><i>→</i><b>Solution</b><i>→</i><b>Security</b><i>→</i><b>Outcome</b></div><Link to={`/projects/${p.slug}`} className="text-link">Read case study <ArrowUpRight size={15}/></Link></article>)}</div></section></>}
