import {useState} from 'react';import {PageHero,SectionHeader} from '../components/Section';import {projects} from '../data/site';import {ProjectCard} from '../components/ProjectCard'
export function Projects(){const [filter,setFilter]=useState('All');const cats=['All','AI','Software Engineering','Full Stack','Backend','Automation','APIs'];const visible=filter==='All'?projects:projects.filter(p=>p.tags.includes(filter)||p.category===filter);return <><PageHero eyebrow="PROJECTS" title="Proof through projects, not buzzwords." description="Explore technical work, product concepts and engineering references. Every project is labeled honestly so visitors can distinguish prototypes from production systems."/><section className="section"><div className="container"><SectionHeader eyebrow="PROJECT LIBRARY" title="Selected work"/><div className="filters">{cats.map(c=><button className={filter===c?'filter active':'filter'} key={c} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="project-grid">{visible.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></div></section></>}
